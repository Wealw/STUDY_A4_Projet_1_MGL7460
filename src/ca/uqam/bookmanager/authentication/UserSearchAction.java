package ca.uqam.bookmanager.authentication;

/**
 * Define all the possible action in th user search menu.
 */
public enum UserSearchAction {
    /**
     * User wants to leave.
     */
    QUIT,
    /**
     * User wants to search by username.
     */
    BY_USERNAME,
    /**
     * User wants to search by role.
     */
    BY_ROLE
}
